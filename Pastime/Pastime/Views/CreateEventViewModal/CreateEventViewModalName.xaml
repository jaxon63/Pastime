<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Pastime.Views.CreateEventViewModal.CreateEventViewModalName">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
            <Label Text="Please enter a name for the event" />
            <Entry Text="{Binding Name}" />
            <Label FontSize="Micro" TextColor="Red" HorizontalOptions="Center" Text="{Binding NameErrMsg}" />
            <Button Text="Next" Command="{Binding NameCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>