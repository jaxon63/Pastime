<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Pastime.Views.EventView">
    <ContentPage.Title>View Event</ContentPage.Title>
        <ContentPage.Content>
            <StackLayout>
                <StackLayout Orientation="Horizontal" HorizontalOptions="Center" Margin="0,10,0,10">
                    <Image Source="{Binding ViewEvent.Activity.IconImage}" />
                    <Label HorizontalOptions="Center" HorizontalTextAlignment="Center" FontSize="Large" Text="{Binding ViewEvent.Name}" />
                </StackLayout>

            <Label Text="About this event" FontAttributes="Bold" />
            <Label Text="{Binding ViewEvent.Description}" />

            <Label Text="When and where?" FontAttributes="Bold" />
            <Label Text="TODO: get locality maybe" />
            <StackLayout Orientation="Horizontal">
                <Label>
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="Attendees: "/>
                            <Span Text="{Binding GuestsAttending}" />
                            <Span Text=" / " />
                            <Span Text="{Binding ViewEvent.MaxGuests}" />
                        </FormattedString>
                    </Label.FormattedText>
                </Label>
            </StackLayout>

            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Button Text="Back" Command="{Binding BackCommand}" />
                <Button Text="Join" />
            </StackLayout>



        </StackLayout>
    </ContentPage.Content>
</ContentPage>